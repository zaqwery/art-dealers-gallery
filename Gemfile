source 'https://rubygems.org'

gem 'rails', '3.2.11'

# Bundle edge Rails instead:
# gem 'rails', :git => 'git://github.com/rails/rails.git'

gem 'pg'
gem 'thin'

gem "rack-cache", :require => "rack/cache"
gem "dragonfly", "~>0.9.12" #images handler
gem "devise" #Flexible authentication solution for Rails with Warden
gem "cancan" #Roles in Rails app
gem "rolify" #Roles Management
gem "transitions", "~> 0.1.1" #Lightweight state machine extracted from ActiveModel
gem "simple_form" #Forms
gem 'acts-as-taggable-on', '~> 2.3.1' #tags

# Gems used only for assets and not required
# in production environments by default.
group :assets do
  gem 'sass-rails',   '~> 3.2.3'
  gem 'coffee-rails', '~> 3.2.1'
  gem "haml-rails"
  gem "compass-rails"

  # See https://github.com/sstephenson/execjs#readme for more supported runtimes
  # gem 'therubyracer', :platforms => :ruby

  gem 'uglifier', '>= 1.0.3'
end

gem 'jquery-rails'

# To use ActiveModel has_secure_password
# gem 'bcrypt-ruby', '~> 3.0.0'

# To use Jbuilder templates for JSON
# gem 'jbuilder'

# Use unicorn as the app server
# gem 'unicorn'

# Deploy with Capistrano
# gem 'capistrano'

# To use debugger
# gem 'debugger'
